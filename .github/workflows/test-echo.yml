on:
  pull_request:
    paths:
      - "echo/**"

jobs:
  test:
    name: Test Echo Binary
    uses: ./.github/workflow/test.yml
    with:
      test-name: echo
      test-args: --node-count 1 --time-limit 10
      binary-name: maelstrom-echo